# reverse-bpf-assembly
This project provides a user-friendly GUI for reverse-engineering **compiled BPF (Berkeley Packet Filter)** output into its original, human-readable syntax using OpenAI's o1 model.
